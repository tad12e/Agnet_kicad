"""Power symbol management for KiCad schematic — FUTURE STUB.

Will implement:
    sch.power.add(name="+5V", position=(x, y))
    sch.power.add(name="GND", position=(x, y))

Power symbols in KiCad are special symbols from the "power" library
with the (power) flag set. They create implicit global connections.
"""
